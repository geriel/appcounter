/*
 * grunt-remote
 * https://github.com/guilherme/grunt-remote
 *
 * Copyright (c) 2013 Guilherme Rodrigues
 * Licensed under the MIT license.
 */

'use strict';

module.exports = function(grunt) {

  // Project configuration.
  grunt.initConfig({
    // Configuration to be run (and then tested).
    remote: {
      default_options: {
        options: {
        }
      },
      custom_options: {
        options: {
					"remote": {
						"host": "dontpad.com",
						"port": 80
					},
					"proxy": {
						"host": "localhost",
						"port": 4444
					},
					"headers": {
					},
					"bounces":[
						"lib/.*",
						"js/.*",
						"style/.*",
						"/$",
						"index.html"
					],
					"mapping": true,
					"mappings": {
						"test":"http://dontpad.com"
					}
				}
      }
    }
  });

  // Actually load this plugin's task(s).
  grunt.loadTasks('tasks');

  // By default, lint and run all tests.
  grunt.registerTask('default', ['remote']);
};
